#include<stdio.h>
int main()
{
    int t;
    int ar[10000];
    scanf("%d",&t);
    while(t--){
        int n;
        scanf("%d",&n);
        for(int i=0; i<n; i++){
            scanf("%d",&ar[i]);
        }

        int b[n];
        int c[n+1];
        for(int i=0; i<=n; i++){
            c[i]=0;
        }
        for(int i=0; i<n; i++){
            b[i]=ar[i];

            if()
        }
    }

    return 0;
}
